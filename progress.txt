✅ COMPLETED: Recovered functionality for -v flag to print colorized normal output of opencode as it executes. Updated ralph.go to use io.MultiWriter to capture output while displaying it when verbose mode is enabled. The implementation preserves colorized output and allows inspection without running opencode multiple times.

✅ COMPLETED: Implemented tee reader functionality so output can be inspected without running opencode multiple times. Using bytes.Buffer to capture output and io.MultiWriter to simultaneously capture and display output when verbose mode is enabled. This eliminates the need for multiple opencode calls in a single loop iteration.

✅ COMPLETED: Added -debug flag that adds --print-logs to opencode command. The debug flag implies verbose mode and automatically adds the --print-logs argument to the opencode command for enhanced debugging information.

✅ COMPLETED: Ensured opencode is not called multiple times in one loop. Replaced the previous implementation that called opencode twice per iteration with a single call that captures output for both display and inspection.

✅ COMPLETED: Tested the implementation to ensure proper output handling. Verified that verbose mode shows colorized output, debug mode shows logs, and the completion signal detection works correctly from captured output.