
- send a message when all iterations are done to a webhook. Can config it to any hook
- Have the ability to wait for a reply that is added into the next loop or the tasks.json depending on the response from the user. ex if the user responds i need x y z and q add them to the tasks.md then start next iteration
- have the ability to run in  a docker container with folder mounted. The docker container will need to have a full dev environment for the languaguage of the project. Or it will start from ubuntu and install as need in the container.
